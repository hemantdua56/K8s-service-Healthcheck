apiVersion: v1
kind: Service
metadata:
  name: {{ include "healthcheck-backend.fullname" . }}
  labels:
    {{- include "healthcheck-backend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "healthcheck-backend.selectorLabels" . | nindent 4 }}
