apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "healthcheck-backend.fullname" . }}-test-connection"
  labels:
    {{- include "healthcheck-backend.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "healthcheck-backend.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
